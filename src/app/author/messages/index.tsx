import { Room } from "@/interfaces/messages";

interface AuthorMessagesProps {
    rooms: Array<Room>;
}

export default function AuthorMessages() {
    return (
        <div>
            
        </div>
    )
}